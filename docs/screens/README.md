# Stale captures

These screenshots predate `/platby`, the current sheet, the tape as it stands,
and the 2026-08-29 audit's colour retunes. They are kept as a record of earlier
passes — **trust the code and `tokens.css` over them.**
